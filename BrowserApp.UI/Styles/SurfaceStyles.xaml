<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Style x:Key="PanelHeaderCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BgGlassHeaderBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="14,12"/>
        <Setter Property="Margin" Value="8"/>
    </Style>

    <Style x:Key="PanelBodyCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BgSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="8,0,8,8"/>
    </Style>

    <Style x:Key="GlassToolbarButtonStyle" TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
        <Setter Property="Appearance" Value="Secondary"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Margin" Value="3,0"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource BgSurfaceHoverBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource BgSurfaceActiveBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PillInputContainerStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BgSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="24"/>
        <Setter Property="Padding" Value="4,2"/>
    </Style>

    <Style x:Key="ModernDialogWindowStyle"
           TargetType="ui:FluentWindow"
           BasedOn="{StaticResource {x:Type ui:FluentWindow}}">
        <Setter Property="Background" Value="{StaticResource BgPrimaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="ShowInTaskbar" Value="False"/>
        <Setter Property="WindowStartupLocation" Value="CenterOwner"/>
    </Style>

    <Style x:Key="ModernManagerWindowStyle"
           TargetType="ui:FluentWindow"
           BasedOn="{StaticResource ModernDialogWindowStyle}">
        <Setter Property="Width" Value="1100"/>
        <Setter Property="Height" Value="760"/>
        <Setter Property="MinWidth" Value="860"/>
        <Setter Property="MinHeight" Value="560"/>
        <Setter Property="ResizeMode" Value="CanResize"/>
    </Style>

    <Style x:Key="CardListItemStyle" TargetType="ListViewItem" BasedOn="{StaticResource ThemedListViewItem}">
        <Setter Property="Padding" Value="8"/>
        <Setter Property="Margin" Value="4,3"/>
    </Style>
</ResourceDictionary>
