<ui:FluentWindow x:Class="BrowserApp.UI.Views.MarketplaceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Marketplace"
        WindowStartupLocation="CenterOwner"
        Loaded="MarketplaceView_Loaded"
        ExtendsContentIntoTitleBar="True"
        Style="{StaticResource ModernManagerWindowStyle}">

    <Grid Margin="16" Style="{StaticResource FadeInOnLoadModern}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Marketplace" ShowMinimize="True" ShowMaximize="True" ShowClose="True"/>

        <!-- Header -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="Marketplace" Style="{StaticResource TitleTextStyle}" Margin="0,0,0,4"/>
            <TextBlock Text="Browse and install community rules" Foreground="{StaticResource TextSecondaryBrush}"/>
        </StackPanel>

        <!-- Search Bar -->
        <Border Grid.Row="2" Background="{StaticResource BgSurfaceBrush}" CornerRadius="8"
                BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1"
                Padding="8,6" Margin="0,0,0,8">
            <ui:TextBox PlaceholderText="Search marketplace rules..."
                        Text="{Binding SearchFilter, UpdateSourceTrigger=PropertyChanged}"
                        Icon="{ui:SymbolIcon Search24}" BorderThickness="0" Background="Transparent"/>
        </Border>

        <!-- Stats and Actions -->
        <Grid Grid.Row="3" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{Binding TotalRules}" FontWeight="SemiBold"/>
                <TextBlock Text=" rules available" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:Button Content="Refresh" Icon="{ui:SymbolIcon ArrowSync24}" Command="{Binding LoadRulesCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Rules List -->
        <Border Grid.Row="4" Style="{StaticResource PanelBodyCardStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding Rules}"
                      Style="{StaticResource ThemedDataGrid}"
                      SelectedItem="{Binding SelectedRule}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0"
                      Background="Transparent">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="180"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
                    <DataGridTextColumn Header="Site" Binding="{Binding Site}" Width="120"/>
                    <DataGridTextColumn Header="Author" Binding="{Binding AuthorUsername}" Width="100"/>
                    <DataGridTemplateColumn Header="Downloads" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <ui:SymbolIcon Symbol="ArrowDownload24" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="0,0,4,0"/>
                                    <TextBlock Text="{Binding DownloadCount}" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ui:Button Content="{Binding InstallButtonText}"
                                           Appearance="Primary"
                                           Command="{Binding DataContext.InstallRuleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding}"
                                           IsEnabled="{Binding IsInstalled, Converter={StaticResource InverseBoolConverter}}"
                                           Padding="8,4"
                                           HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Loading Indicator -->
        <Border Grid.Row="4"
                Background="{StaticResource BgPrimaryBrush}"
                Opacity="0.82"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
            <ui:ProgressRing IsIndeterminate="True"/>
        </Border>

        <!-- Status Message -->
        <TextBlock Grid.Row="5"
                   Text="{Binding StatusMessage}"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Margin="0,8,0,0"
                   TextWrapping="Wrap"/>

    </Grid>
</ui:FluentWindow>


